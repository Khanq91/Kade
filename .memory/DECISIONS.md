# DECISIONS — Kade

Append-only. Đọc hết trước khi hỏi user bất cứ câu "nên chọn A hay B" nào.
Format và quy tắc ghi: xem `AGENTS.md` §4.

## 2026-09-09 — D001 — Tên app: Kade
- Bởi: user
- Quyết định: app tên "Kade". Package Flutter `apps/kade`, Android applicationId do user chốt khi tạo project (xem PROGRESS "Cần user làm").
- Lý do: convention đặt tên của user.
- Hệ quả: không.

## 2026-09-09 — D002 — Target MVP: web + Android, iOS lùi phase sau
- Bởi: user
- Quyết định: MVP build web (PWA) và Android. iOS (app + WidgetKit) là Phase 5.
- Lý do: ưu tiên nhanh; iOS widget tốn công hơn.
- Hệ quả: `platform/` vẫn tách interface để sau thêm iOS không phải sửa UI. Không viết code iOS ở MVP.

## 2026-09-09 — D003 — Stack
- Bởi: user (theo đề xuất)
- Quyết định: Riverpod (không codegen), go_router, Hive CE, freezed + json_serializable, home_widget, flutter_local_notifications + timezone, google_sign_in + googleapis.
- Lý do: chạy được cả web lẫn Android không cần native setup thêm; Hive CE có IndexedDB.
- Hệ quả: không dùng Isar/Drift/sqflite.

## 2026-09-09 — D004 — Export/Import JSON trong MVP
- Bởi: user
- Quyết định: có Export/Import file JSON, format = `SyncEnvelope` (plan §2.5), dùng chung với file sync trên Drive.
- Lý do: user web không đăng nhập Google vẫn có cách giữ dữ liệu.
- Hệ quả: 1 serializer cho cả 2 tính năng.

## 2026-09-09 — D005 — Quy tắc tháng nhuận mặc định: firstMonth
- Bởi: user
- Quyết định: sự kiện âm lịch lặp hàng năm rơi vào năm có tháng nhuận trùng tháng → mặc định lấy tháng chính (tháng đầu). User đổi được từng sự kiện (`LeapMonthRule`: firstMonth | secondMonth | both).
- Lý do: tục lệ phổ biến.
- Hệ quả: không.

## 2026-09-09 — D006 — Ngày lễ quốc tế: ~10 ngày
- Bởi: user
- Quyết định: danh sách ở plan §2.2. Ngày của Mẹ/Cha (rule "CN thứ N của tháng") CHƯA chốt — agent hỏi user khi làm Phase 1 bước 4, hoặc bỏ nếu user không trả lời.
- Lý do: tránh nhồi.
- Hệ quả: `Event` có thể cần field `nthWeekday` nếu giữ 2 ngày này.

## 2026-09-09 — D007 — Sync qua Google Drive appDataFolder, trong MVP
- Bởi: user (hỏi Google Sheets trước, chọn appDataFolder theo đề xuất)
- Quyết định: sync sự kiện cá nhân = 1 file `kade_events.json` trong appDataFolder của user, scope `drive.appdata`. Không dùng Sheets, không backend riêng. Merge theo `updatedAt`, last-write-wins, tombstone `deletedAt`.
- Lý do: scope non-sensitive không cần verify; blob JSON = format Export/Import; không dính rate limit Sheets.
- Hệ quả: `UserEvent` có `deletedAt`; xóa = soft delete; purge tombstone > 90 ngày sau khi đã sync.

## 2026-09-09 — D008 — Nghỉ bù theo năm: remote config từ Google Sheet qua Apps Script
- Bởi: user
- Quyết định: user sở hữu 1 Sheet tab `overrides`; Apps Script `doGet` trả JSON; app fetch ≤ 1 lần/24h, cache Hive, fallback `assets/overrides.json`. Setup: `docs/setup-google.md`.
- Lý do: sửa nghỉ bù không cần release.
- Hệ quả: nếu CORS từ Flutter web lỗi (verify Phase 1 bước 5) → đổi nguồn sang GitHub raw JSON, Sheet chỉ là nơi soạn. Ghi DECISIONS mới khi đó.

## 2026-09-09 — D009 — Engine: port `docs/reference/amlich-aa98.js` (Hồ Ngọc Đức), múi giờ cố định +7
- Bởi: user (theo đề xuất)
- Quyết định: `lunar_core` port 1:1 từ reference, giữ tên hàm để dễ đối chiếu. Fixture `tet_1900_2100.json` sinh từ chính reference → test port khớp reference 100%.
- Lý do: thuật toán chuẩn de facto của lịch VN.
- Hệ quả: **License reference là "personal, non-commercial use"** (xem header file). Nếu Kade sẽ thu tiền / có quảng cáo → cần user quyết định: (a) xin phép tác giả, (b) viết lại từ Meeus độc lập, (c) giữ non-commercial. → MỞ, hỏi user trước Phase 3 release. Fixture là output của thuật toán, không phải nguồn độc lập; sanity check ngoài: `docs/reference/README.md`.

## 2026-09-09 — D010 — Memory cho agent: `.memory/` 3 file
- Bởi: user
- Quyết định: mọi agent đọc `.memory/PROGRESS.md`, `DECISIONS.md`, `ERRORS.md` đầu session và ghi theo quy tắc `AGENTS.md` §4.
- Lý do: nhiều agent (Claude Code, Codex) thay phiên làm cùng repo.
- Hệ quả: `CLAUDE.md` chỉ import `AGENTS.md`.

## 2026-09-10 — D011 — Root pubspec = workspace root; scaffold `flutter create` ở root giữ nguyên chờ user
- Bởi: agent
- Quyết định: `pubspec.yaml` root đổi từ app Flutter scaffold (commit "PROJECT UPLOAD") thành workspace root: `workspace: [packages/*]`; `apps/*` để comment, bật khi có `apps/kade`. Các file scaffold còn lại ở root (`lib/main.dart`, `test/widget_test.dart`, `android/`, `web/`, `.metadata`, `README.md`, `analysis_options.yaml`) KHÔNG xóa, KHÔNG move.
- Lý do: plan §1 đặt app ở `apps/kade`; user dặn bước 1 chưa tạo `apps/kade`; xóa hay move scaffold (kèm `android/` applicationId `com.kade.kade`) là việc user quyết.
- Hệ quả: `dart analyze` / `dart test` chạy ở root sẽ lỗi vì `lib/main.dart` import flutter mà root không có dep → chỉ chạy trong từng package (`cd packages/lunar_core`). Việc chờ user ghi ở PROGRESS "Cần user làm".

## 2026-09-10 — D012 — API `lunar_core` bước 1
- Bởi: agent
- Quyết định: `lib/src/amlich.dart` giữ nguyên tên hàm JS kể cả viết hoa (`NewMoon`, `SunLongitude`, `INT`) với `ignore_for_file: non_constant_identifier_names`; mảng JS → record cùng thứ tự. API public (`lib/lunar_core.dart`): `LunarDate{day, month, year, isLeapMonth}`; `solarToLunar(DateTime, {double timeZone = 7})` chỉ dùng y/m/d của input; `lunarToSolar(LunarDate, {timeZone = 7})` trả `DateTime.utc` 0h, hoặc `null` nếu ngày âm không tồn tại (kiểm bằng round-trip `solarToLunar(kết quả) == input`, thay cho `[0,0,0]` của JS).
- Lý do: tên 1:1 để đối chiếu từng hàm; `DateTime.utc` tránh lệch DST/local; `null` để UI converter (Phase 1) dùng thẳng, không cần check `[0,0,0]`.
- Hệ quả: app khi so ngày phải so theo y/m/d hoặc dùng `DateTime.utc`, không so trực tiếp với `DateTime.now()` local. `timeZone` kiểu `double` (theo reference), app luôn dùng mặc định 7.

## 2026-09-10 — D013 — (MỞ) Bug "ngày 0" trong reference: giữ 1:1 hay sửa
- Bởi: agent (chờ user chốt)
- Quyết định: TẠM giữ port 1:1 theo D009 → port tái hiện đúng quirk của reference. Test `reference_quirks_test.dart` ghim 2 ngày này và sẽ đỏ khi sửa. Đề xuất SỬA: thêm 1 dòng sau fallback hiện có trong `convertSolar2Lunar` — `if (monthStart > dayNumber) monthStart = getNewMoonDay(k - 1, timeZone);` — ở CẢ `docs/reference/amlich-aa98.js` và port, rồi `node tools/gen_fixture.js` sinh lại fixture, sửa README + xóa quirk test. Cần chốt trước Phase 0 bước 3 (bảng const sẽ đóng băng kết quả).
- Lý do: reference trả `0/4/2054` cho 2054-05-07 và `0/3/2062` cho 2062-04-09 (JS gốc chạy node cho cùng kết quả — chi tiết ERRORS E005). Theo chính sóc mà thuật toán tính ra, đúng phải là 30/3/2054 và 30/2/2062. Sửa = lệch D009 và đổi reference (README nói "không nên") → không tự quyết.
- Hệ quả: nếu giữ, app hiện "ngày 0" ở 2 ngày trên. Nếu sửa: fixture 2054 (tháng 3: 29→30 ngày, tháng 4 start 05-07→05-08, 30→29) và 2062 (tháng 2: 29→30, tháng 3 start 04-09→04-10, 30→29) đổi.

## 2026-09-10 — D014 — Chốt D013: SỬA bug "ngày 0" trong reference + port, sinh lại fixture
- Bởi: user ("ok theo đề xuất")
- Quyết định: thêm 1 lần lùi sóc trong `convertSolar2Lunar` (`if (monthStart > dayNumber) monthStart = getNewMoonDay(k-1, timeZone)`) ở cả `docs/reference/amlich-aa98.js` (đánh dấu `Kade patch`) và `packages/lunar_core/lib/src/amlich.dart`. Fixture `tet_1900_2100.json` sinh lại bằng `node tools/gen_fixture.js` từ JS đã patch. README reference ghi mục "Patch". Xóa `reference_quirks_test.dart`, thay bằng `reference_patch_test.dart` (2054-05-07 = 30/3/2054, 2062-04-09 = 30/2/2062, mọi ngày day ∈ 1..30).
- Lý do: ngày âm 0 là sai hiển nhiên; patch chỉ mở rộng fallback sẵn có, không đổi công thức thiên văn.
- Hệ quả: D009 "port 1:1" nay hiểu là 1:1 với reference ĐÃ PATCH (1 chỗ, có đánh dấu). Fixture 2054 (tháng 3: 30 ngày, tháng 4 start 05-08) và 2062 (tháng 2: 30 ngày, tháng 3 start 04-10) đổi; các năm khác giữ nguyên.

## 2026-09-10 — D015 — Scaffold Flutter ở root là project của user → chuyển vào `apps/kade`
- Bởi: user (xác nhận "project t tự tạo trong Android Studio"), agent thực hiện
- Quyết định: `git mv` toàn bộ scaffold (`lib/`, `test/`, `android/`, `web/`, `.metadata`, `README.md`) vào `apps/kade/`; khôi phục `apps/kade/pubspec.yaml` từ commit `5f8f34a` + thêm `resolution: workspace`; copy `analysis_options.yaml`; root `pubspec.yaml` bật `apps/*`. Không đổi applicationId (`com.kade.kade`) lúc này — user đã chốt `vn.kade.kade`, làm ở Phase 2 bước 10 như PROGRESS ghi.
- Lý do: plan §1 đặt app ở `apps/kade`; root không thể vừa là workspace root vừa là app Flutter; giữ `android/`, `web/` do user tạo thay vì `flutter create` lại.
- Hệ quả: Sửa D011 (scaffold không còn ở root). Lệnh `dart pub get` ở root vẫn chạy được với app Flutter trong workspace (Dart của Flutter SDK), `flutter pub get` cũng được. Verify: `flutter analyze` + `flutter test` trong `apps/kade` pass.

## 2026-09-10 — D016 — Quy ước bước 2: can chi, tiết khí, hoàng đạo, DayInfo (chờ user đối chiếu)
- Bởi: agent (giá trị cụ thể chờ user xác nhận bằng lịch vạn niên)
- Quyết định: (1) Can chi theo công thức Hồ Ngọc Đức bản web: năm `(y+6)%10, (y+8)%12`; tháng `(y*12+m+3)%10, (m+1)%12`, tháng nhuận cùng can chi tháng chính (không gắn "(nhuận)" vào chuỗi, UI tự thêm); ngày `(jd+9)%10, (jd+1)%12`; giờ `((jd-1)*2 + chiGiờ)%10`. (2) Tiết khí: 24 mốc 15°, tính `SunLongitude` tại 0h địa phương (+7); `tietKhiOf(d)` trả tên tiết khí mới nếu chỉ số ở 0h ngày d+1 khác 0h ngày d (Mặt Trời vượt mốc trong ngày d → ngày d là ngày đầu tiết). Danh sách tên bắt đầu Xuân phân. (3) Giờ hoàng đạo: bảng 12 dòng theo chi ngày (GIO_HD của HND), định dạng "Tý (23-1)". (4) Ngày hoàng đạo: bảng 12×12 chi tháng × chi ngày, sinh từ vòng 12 thần với Thanh Long khởi Tý ở tháng Dần, +2 chi mỗi tháng; 6 thần hoàng đạo: Thanh Long, Minh Đường, Kim Quỹ, Kim Đường, Ngọc Đường, Tư Mệnh. Có `thanOfDay()` trả tên thần. (5) `DayInfo` immutable, `solar` chuẩn hóa về `DateTime.utc` y/m/d.
- Lý do: các công thức là chuẩn phổ biến của lịch VN; bảng ngày hoàng đạo viết tường minh để user dò từng dòng, có test kiểm bảng == vòng 12 thần.
- Hệ quả: test hiện tại chỉ kiểm cấu trúc (chu kỳ 60, 24 tiết khí/năm, 6 giờ HĐ/ngày...). Sau khi user xác nhận output `example/day_info_example.dart` → thêm test giá trị cố định, commit "phase0-step2: ...". Nếu user báo lệch → sửa bảng/công thức, ghi DECISIONS mới.

## 2026-09-10 — D017 — User xác nhận output bước 2 → test giá trị cố định
- Bởi: user ("ổn thì tiếp tục" sau khi tự chạy `example/day_info_example.dart` và xem 10 ngày + 24 tiết khí 2025)
- Quyết định: chốt công thức/bảng của D016. Toàn bộ output (10 ngày: âm lịch, can chi năm/tháng/ngày/giờ Tý, tiết khí, 6 giờ hoàng đạo, ngày hoàng đạo + thần; 24 ngày tiết khí 2025) thành test cố định `test/day_info_fixed_test.dart`.
- Lý do: theo prompt bước 2, chỉ chốt fixture sau khi user đối chiếu lịch vạn niên.
- Hệ quả: đổi bất kỳ công thức/bảng nào ở can_chi / tiet_khi / hoang_dao → test này đỏ → cần DECISIONS mới + user xác nhận lại.

## 2026-09-10 — D018 — Runtime tra bảng const 1900–2100 khi múi giờ 7, ngoài đó tính trực tiếp
- Bởi: agent (theo đề xuất trong prompt bước 3)
- Quyết định: `tools/gen_lunar_table.dart` (chạy ở root: `dart run tools/gen_lunar_table.dart && dart format packages/lunar_core`) sinh `packages/lunar_core/lib/src/table_1900_2100.dart`: 201 entry `(JD mùng 1 Tết, tháng nhuận | 0, bitmask 13 bit tháng 30 ngày)`. `solarToLunar`/`lunarToSolar` public: nếu `timeZone == 7` và ngày/năm nằm trong bảng → tra bảng (`lib/src/lunar_table.dart`), ngược lại → `solarToLunarComputed`/`lunarToSolarComputed` (thuật toán, vẫn export). Root `pubspec.yaml` thêm `dev_dependencies: lunar_core: any` để script trong `tools/` import được và analyze sạch.
- Lý do: (1) kết quả cố định, không phụ thuộc `sin()` của VM / dart2js / wasm ở biên nửa đêm; (2) nhanh hơn nhiều cho MonthView/Upcoming quét nhiều ngày; (3) vẫn chạy được ngoài 1900–2100 và múi giờ khác nhờ fallback.
- Hệ quả: đổi thuật toán (amlich.dart) → phải chạy lại generator; `test/lunar_table_test.dart` so bảng == runtime cho mọi ngày 1900–2100 và mọi tổ hợp (năm, tháng, nhuận, ngày 1–30) nên quên sinh lại sẽ đỏ. Fixture test (`fixture_test.dart`) giờ đi qua đường bảng; đường tính trực tiếp được phủ bởi test bảng == runtime.

## 2026-09-10 — D019 — Chốt D006 + nội dung 3 danh sách sự kiện app (`calendar_data`)
- Bởi: user (D006: "giữ ngày mẹ/cha"); agent đề xuất danh sách
- Quyết định: (1) `Event` thêm `NthWeekday? nthWeekday` (thứ X lần thứ N trong tháng, chỉ lịch dương); Ngày của Mẹ = CN thứ 2 tháng 5, Ngày của Cha = CN thứ 3 tháng 6. (2) `vnHolidays` = ngày nghỉ cố định theo Bộ luật Lao động 2019 Điều 112: Tết Dương lịch 1/1, Tết Nguyên đán mùng 1–3 (durationDays 3 theo plan §2.2), Giỗ Tổ 10/3 âm, 30/4, 1/5, 2/9. Ngày nghỉ thêm theo năm (30 Tết, mùng 4–5, ngày thứ 2 Quốc khánh, nghỉ bù) đi qua YearOverride, không hardcode. (3) `international` đúng plan §2.2 trừ "Năm mới 1/1" gộp vào "Tết Dương lịch" (vnHoliday) để không hiện 2 dòng cùng ngày → 9 mục. (4) `vnMemorials` (22 mục, KHÔNG ảnh hưởng ngày nghỉ) là ĐỀ XUẤT, chờ user duyệt: dương 3/2, 27/2, 8/3, 26/3, 7/5, 19/5, 21/6, 28/6, 27/7, 19/8, 10/10, 13/10, 20/10, 20/11, 22/12; âm 15/1, 3/3, 15/4, 5/5, 15/7, 15/8, 23/12.
- Lý do: plan chỉ liệt kê danh sách quốc tế và vài ví dụ (30/4, 2/9, Tết, Giỗ Tổ, Trung Thu, 20/10); danh sách nghỉ là luật, danh sách kỷ niệm là lựa chọn biên tập → user quyết.
- Hệ quả: user thêm/bớt mục trong `packages/calendar_data/lib/src/events.dart` chỉ là sửa dữ liệu; test `events_test.dart` kiểm cấu trúc, `resolve_month_test.dart` dùng vài id cụ thể (thanh-lap-dang, thay-thuoc, ram-thang-gieng, doan-ngo) → đổi id thì sửa test.

## 2026-09-10 — D020 — API `calendar_data`: resolveMonth chỉ gộp sự kiện app; sự kiện âm chỉ tháng chính
- Bởi: agent
- Quyết định: `resolveMonth(year, month, {overrides, events})` → `Map<DateTime(utc 0h), DayEvents{date, appEvents, isOffDay}>`; `eventsOn(date)`; `isOffDay` đúng công thức plan §3.2 (`work` thắng, rồi `off`, rồi có vnHoliday). `DayEvents` ở đây KHÔNG có `userEvents` (UserEvent là model app dùng Hive/freezed, không thể nằm trong pure Dart package) — app gộp thêm ở bước 7. Sự kiện âm của app chỉ khớp tháng chính (`!isLeapMonth`), tương tự `firstMonth` của D005. Sự kiện nhiều ngày khớp theo "ngày bắt đầu = hôm nay − k" nên vắt qua tháng/năm vẫn đúng. `YearOverrides` parse chấp nhận `version` số thực (JS), bỏ qua ngày sai định dạng và key lạ (`_note`); ngày là `DateTime.utc`.
- Lý do: pure Dart, không phụ thuộc Flutter/Hive; giữ đúng công thức plan.
- Hệ quả: asset `assets/overrides.json` vẫn ở root repo (test đọc `../../assets/overrides.json`); khi làm bước 5/6 phải đưa vào `apps/kade` (Flutter chỉ bundle asset trong thư mục project) và cập nhật `docs/setup-google.md` nếu có nhắc đường dẫn.

## 2026-09-10 — D021 — Chốt danh sách sự kiện: thêm Giao thừa, giữ đủ 22 kỷ niệm, phân biệt bằng `kind` + `type`
- Bởi: user ("lấy thêm ngày trước 1/1 để đánh dấu giao thừa"; "lấy hết"; "cần phân biệt rõ lễ nào của âm, nào của dương")
- Quyết định: (1) `Event.offsetDays` (mặc định 0): ngày bắt đầu = ngày neo + offsetDays; sự kiện mới `giao-thua` (vnHoliday, âm, neo 1/1, offsetDays −1, 1 ngày) → luôn là 29 hoặc 30 tháng Chạp; `tet` giữ mùng 1–3. (2) `vnMemorials` giữ nguyên 22 mục đã đề xuất (D019). (3) Phân biệt hiển thị dựa trên 2 field có sẵn: `kind` (nghỉ / kỷ niệm / quốc tế) và `type` (âm / dương) — UI bước 6 phải hiện rõ: màu/badge khác cho `vnHoliday` vs `vnMemorial`, và ký hiệu âm/dương (vd. "ÂL"/"DL" hoặc icon) cạnh mỗi sự kiện. Gộp "Năm mới 1/1" vào "Tết Dương lịch" giữ nguyên (user không phản đối; là sự kiện dương).
- Lý do: theo yêu cầu user; Giao thừa không có ngày cố định (tháng Chạp 29/30 ngày) nên cần offset từ mùng 1.
- Hệ quả: `giao-thua` là vnHoliday → `isOffDay` true ngày trước Tết (thực tế lịch nghỉ Nhà nước luôn gồm ngày này). Test cố định: 05/02/2027, 30/01/2014.

## 2026-09-10 — D022 — Kiến trúc bước 5: remote config trong `apps/kade`
- Bởi: agent
- Quyết định: `lib/core/env.dart` đọc `KADE_CONFIG_URL`/`KADE_WEB_CLIENT_ID` bằng `String.fromEnvironment` (từ `--dart-define-from-file=../../dart_defines.json`); `lib/core/strings.dart` gom text UI; `lib/data/local/hive_boxes.dart` mở box `remote_config` (String) + `settings`; `lib/data/remote/remote_config.dart` = `RemoteConfigRepository` (loadLocal: cache → asset → rỗng; fetch: GET timeout 5s, so `version` với cache, lưu `json|version|fetchedAt`, chỉ tự fetch khi quá 24h, `force` bỏ qua; nhận HTML → báo lỗi deploy "Anyone"); `remote_config_provider.dart` = `AsyncNotifier` + `remoteConfigRepositoryProvider` override trong `main()` (DI qua Riverpod override, test override bằng MockClient + Hive temp). Tự fetch nền lúc start nếu quá 24h (plan §3.1). `SettingsScreen` là home tạm (bước 6 thay bằng MonthView) với nút "Kiểm tra cập nhật" + SnackBar kết quả. Asset `apps/kade/assets/overrides.json` khai báo trong pubspec.
- Lý do: pure Dart repo test được không cần Flutter binding; provider override là cách DI không codegen của Riverpod; `AsyncNotifier` cho phép UI `when(loading/error/data)`.
- Hệ quả: MonthView (bước 6) `ref.watch(remoteConfigProvider)` → lấy `overrides` truyền vào `resolveMonth`, cache tháng invalidate khi state đổi. Chưa có nút trong UI để xóa cache (không cần).

## 2026-09-10 — D023 — Kiến trúc bước 6: resolveMonth cấp app, MonthView/DayDetail/Converter, go_router tối thiểu
- Bởi: agent
- Quyết định: (1) `lib/data/month_provider.dart`: `DayCell{info: DayInfo, appEvents, isOffDay}` (chưa có `userEvents`, chừa chỗ bằng comment — bước 7 thêm field + watch provider user events), `MonthData` (map `DateTime.utc` → `DayCell`), `overridesProvider` = `remoteConfigProvider.select(overrides)` (rỗng khi đang load), `monthProvider = Provider.family<MonthData, (int, int)>` (cache theo tháng, tự tính lại khi overrides đổi), `dayCellProvider` đi qua cache tháng. (2) `lib/core/event_style.dart`: `kindColor` (nghỉ lễ đỏ `C62828`, kỷ niệm cam `EF6C00`, quốc tế xanh `1565C0`), nhãn `EventTag` "ÂL"/"DL" nền màu kind (D021); ô nghỉ nền `colorScheme.errorContainer`, hôm nay viền `primary`, mùng 1 hiện "1/M" ("1/MN" nếu nhuận), chú giải dưới lưới. (3) Lưới tháng = Column/Row `Expanded` (không GridView) để vừa màn hình; tuần bắt đầu Thứ Hai; ô trống cho ngày ngoài tháng. Đổi tháng = `context.go('/YYYY/MM')` (URL là nguồn sự thật → reload giữ tháng). Picker tháng dương/âm là `AlertDialog` tự viết (`month_picker.dart`), picker âm liệt kê tháng nhuận như một chip riêng "5 nhuận" (không checkbox). (4) `DayDetail`: thần của ngày = `thanOfDay(chiIndexOfMonth(lunar.month), chi.indexOf(canChiDay.split(' ').last))` — không cần export JD từ engine. Đổi ngày ±1 bằng `pushReplacement` để giữ stack về MonthView; mở thẳng URL thì nút Quay lại `go` về tháng chứa ngày. (5) Converter là `StatefulWidget` thuần (không Riverpod), `initialDate` optional cho test; lỗi âm → dương: thử `lunarToSolar(1/m/y nhuận)` để phân biệt "năm không có tháng nhuận đó" với "ngày không tồn tại". (6) Router: `StatefulShellRoute.indexedStack` 3 branch (Lịch `/:year/:month`, `/convert`, `/settings`) — giữ tháng khi chuyển tab; `/d/:date` là route gốc (`parentNavigatorKey`) khai báo TRƯỚC shell để không bị `/:year/:month` bắt; `/` và path sai → redirect tháng hiện tại. `createRouter({initialLocation})` cho test, `appRouter` cho app; `KadeApp({router})`. (7) Thêm `flutter_localizations`, `locale: vi` để `showDatePicker` tiếng Việt. (8) Giữ hash URL mặc định của Flutter web (`/#/2027/02`); `usePathUrlStrategy` + cấu hình host để bước 14.
- Lý do: URL-driven month là cách rẻ nhất đạt §4.8 mục 2; StatefulShellRoute là cách go_router chuẩn để giữ state tab; Column/Row Expanded tránh overflow trong test 800×600 và điện thoại; tách `event_style` để MonthView và DayDetail dùng chung một quy ước màu/nhãn.
- Hệ quả: bước 7 sửa `DayCell` + `monthProvider` (thêm userEvents), bước 8 thêm branch Upcoming vào `AppShell`, bước 14 đổi URL strategy + responsive + phím tắt. Test widget bước 6 dùng `test/test_app.dart` (`FakeRemoteConfig` extends `RemoteConfigNotifier`, không Hive → không cần `runAsync`).

## 2026-09-10 — D024 — Commit message không có dòng đánh dấu agent
- Bởi: user
- Quyết định: mọi agent KHÔNG tự thêm `Co-Authored-By:`, "Generated with …" hay marker tương tự vào commit message / PR. Ghi vào `AGENTS.md` §3.
- Lý do: yêu cầu của user cho toàn bộ repo.
- Hệ quả: hướng dẫn attribution mặc định của tool (nếu có) bị bỏ qua trong repo này.

## 2026-09-10 — D025 — Kiến trúc bước 7: UserEvent freezed, lưu Hive dạng JSON, tombstone, khớp leapRule
- Bởi: agent
- Quyết định: (1) `lib/data/models/user_event.dart`: `UserEvent` là freezed class đúng field plan §2.4 + `LeapMonthRule {firstMonth, secondMonth, both}`; `CalendarType` dùng lại của `calendar_data`; JSON qua json_serializable (enum theo tên, DateTime ISO-8601 UTC). Generated `*.freezed.dart`/`*.g.dart` bị gitignore (đã có sẵn trong `.gitignore` root) → sau khi clone/pull phải chạy `dart run build_runner build` trong `apps/kade`. (2) Hive: box `user_events` là `Box<String>`, key = id, value = `jsonEncode(e.toJson())` — KHÔNG dùng TypeAdapter/hive_ce_generator. Đổi schema sau này = đổi JSON (json_serializable default/nullable) + migration đọc-ghi lại, không cần typeId. (3) `UserEventRepository` chỉ `all/get/put/putAll`, không xóa cứng. `UserEventsNotifier` (Riverpod `Notifier<List<UserEvent>>`, state gồm cả tombstone) có `create` (uuid v4, `createdAt = updatedAt = now` UTC), `update` (`updatedAt = now`), `remove` (`deletedAt = updatedAt = now`, D007). `activeUserEventsProvider` lọc tombstone + sắp tháng/ngày/tên; `userEventByIdProvider`. Purge tombstone > 90 ngày "đã sync" → bước 12 (chưa có sync nên chưa purge). (4) Khớp ngày `lib/data/user_event_match.dart` (pure): duration k ngày như `eventsOn` của calendar_data; âm lịch theo D005: tháng nhuận chỉ khớp khi rule `secondMonth`/`both`; tháng chính khớp trừ khi rule `secondMonth` VÀ năm đó có tháng nhuận cùng số (`leapMonthOf`, `lib/core/lunar_utils.dart`); `year != null` → chỉ năm đó. (5) `DayCell.userEvents`; `monthProvider` watch thêm `activeUserEventsProvider` → thêm/sửa/xóa là cache tháng tính lại. (6) UI: DayDetail có mục "Sự kiện cá nhân" + "+ Thêm sự kiện" (`/events/new?date=`); form `/events/new`, `/events/:id` (route gốc đè shell, `UserEventEditScreen` tra id); danh sách `/events` mở từ Cài đặt → "Sự kiện của tôi" (plan §5.1, không vào bottom nav); MonthView ô có nhãn ÂL/DL màu tự chọn (8 màu `userEventColors`, `colorIndex`); form: tên bắt buộc, dương/âm, ngày/tháng/năm + "Lặp hàng năm" (year null), rule nhuận chỉ khi âm (một lần: không có "Cả hai"), số ngày, ghi chú, màu; validate ngày tồn tại khi có năm. `remindBeforeDays` có trong model nhưng chưa có trong form (bước 16). (7) Test widget: box in-memory `Hive.openBox(bytes: Uint8List(0))` (vẫn cần `Hive.init` 1 lần) → không IO, không cần `runAsync`; `testApp()` giờ là async.
- Lý do: JSON-in-Hive tránh codegen adapter và trùng format với SyncEnvelope/Export (D004); freezed theo D003; giữ tombstone trong state để bước 9/12 dùng thẳng; match tách pure để test nhanh 2025 nhuận tháng 6.
- Hệ quả: bước 9 `SyncEnvelope.events = repo.all()` (kể cả tombstone); bước 12 merge theo `updatedAt` rồi `putAll`; bước 16 thêm `remindBeforeDays` vào form. Đổi field `UserEvent` → theo AGENTS §3 (DECISIONS + migration + test).

## 2026-09-10 — D026 — Kiến trúc bước 8: "Sắp tới" quét cache tháng, 4 lớp lưu box settings
- Bởi: agent
- Quyết định: (1) `lib/data/models/event_layer.dart`: `EventLayer {vnHoliday, vnMemorial, international, personal}` (3 kind app + cá nhân, plan §3.6 "4 lớp"). (2) `lib/data/settings_provider.dart`: `settingsBoxProvider` (`Box<dynamic>` `settings`, override trong `main()`), `LayersNotifier` lưu key `layers` = list tên enum, mặc định bật hết; `toggle()` ghi ngay. (3) `lib/data/upcoming_provider.dart`: `todayProvider` (0h UTC theo y/m/d máy, tính lúc tạo — chưa tự đổi qua nửa đêm; test override), `upcomingProvider` quét `upcomingWindowDays = 60` ngày kể cả hôm nay qua `monthProvider` (cache, đã có userEvents), chỉ giữ ngày có sự kiện thuộc lớp bật; sự kiện nhiều ngày chỉ hiện ở ngày bắt đầu (kiểm bằng "không có trong ô ngày hôm trước"), hoặc ở "Hôm nay" nếu đang diễn ra. (4) `UpcomingScreen` (`/upcoming`): 4 `FilterChip` + danh sách gom theo ngày, header "Hôm nay / Ngày mai / Còn N ngày · Thứ, dd/mm/yyyy · d/m ÂL", dòng = nhãn ÂL/DL màu + tên + lớp (+ "N ngày"), tap → DayDetail. (5) Bottom nav theo plan §5.1: [Lịch] [Sắp tới] [Đổi ngày] [Cài đặt] (branch index 1). (6) Lớp hiển thị CHỈ lọc "Sắp tới" (đúng chữ plan §3.6); MonthView/DayDetail vẫn hiện hết. Mục "Cài đặt → Lớp hiển thị" (plan §5.1) chưa làm — chip trên màn Sắp tới đã đủ; nếu user muốn lớp áp cho cả lịch tháng thì ghi DECISIONS mới rồi cho `monthProvider`/`DayTile` đọc `layersProvider`.
- Lý do: tận dụng cache tháng có sẵn thay vì resolveDay riêng; settings box đã mở từ bước 5; chip trên chính màn hình là nơi user cần nhất.
- Hệ quả: bước 14 (panel phải web) dùng lại `upcomingProvider`; bước 16/17 (notification, widget) đọc `upcomingProvider`/`layersProvider` cho "sự kiện hôm nay"; nếu app mở qua nửa đêm thì cần `ref.invalidate(todayProvider)` khi resume (làm ở bước 13 cùng "app resume" hoặc bước 16).

## 2026-09-10 — D027 — Import JSON ghi đè theo id, `updatedAt = now`; N báo user = sự kiện còn hiệu lực
- Bởi: agent (theo prompt bước 9 của user)
- Quyết định: (1) Import = `UserEventRepository.putAll` ghi đè theo `id`, KHÔNG so `updatedAt` với bản local — để "export → xóa hết → import → giống hệt" đúng dù local đang giữ tombstone mới hơn. (2) Mọi bản import nhận `updatedAt = now` (UTC) để bước 12 (merge theo `updatedAt`, D007) không bị bản cũ trên Drive đè lại; `createdAt`, `deletedAt` giữ nguyên từ file (tombstone trong file vẫn được nhập để sync mang đi). (3) Thực hiện qua `UserEventsNotifier.importAll(events, {now})`: putAll rồi `state = repo.all()` (tương đương invalidate `userEventsProvider`) → `activeUserEventsProvider`, cache tháng, "Sắp tới" tự tính lại. (4) Số N trong "Nhập N sự kiện từ file?" / "Đã xuất N" / "Đã nhập N" = `SyncEnvelope.activeCount` (không tính tombstone) — user không thấy tombstone nên không đếm.
- Lý do: tiêu chí verify §6 bước 9; tránh mất dữ liệu khi sync; số đếm khớp với những gì user thấy trong "Sự kiện của tôi".
- Hệ quả: bước 12 coi import như một lần sửa (bản local mới nhất). Không có merge khi import; user được cảnh báo "Sự kiện trùng id sẽ bị ghi đè".

## 2026-09-10 — D028 — Kiến trúc bước 9: SyncEnvelope freezed, FileIo bọc file_picker 12, mục "Sao lưu"
- Bởi: agent
- Quyết định: (1) `lib/data/models/sync_envelope.dart`: `SyncEnvelope` freezed + json đúng plan §2.5 (`schema` mặc định `currentSchema = 1`, `exportedAt` ISO UTC, `deviceId`, `events` = `UserEventRepository.all()` kể cả tombstone); `encode()` JSON thụt lề 2 khoảng; `fileName(now)` = `kade_events_YYYY-MM-DD.json` theo ngày máy; `parse(text)` trả sealed `EnvelopeParse` (`EnvelopeOk` | `EnvelopeError(message)` tiếng Việt: JSON hỏng / không phải object, thiếu `events`, sự kiện sai định dạng / `schema` khác 1) — không throw ra UI. (2) `deviceIdProvider` (`lib/data/settings_provider.dart`): uuid v4 sinh một lần, lưu box settings key `deviceId`. (3) `lib/platform/file_io.dart`: interface `FileIo { pickJson(); saveJson(name, content) }`; bản thật `FilePickerFileIo` dùng `file_picker` ^12.2.0 (`FilePicker.pickFile(type: custom, allowedExtensions: ['json'])` + `readAsBytes()`; `FilePicker.saveFile(fileName, bytes, mimeType: application/json)`); `fileIoProvider` override trong `main()`, test override `FakeFileIo` (trong `test/test_app.dart`, mặc định cho mọi `testApp`/`testOverrides`). Không viết fallback anchor riêng: `file_picker_web` đã tải qua anchor bằng `package:web` (E015). (4) UI: `_BackupSection` trong `SettingsScreen` ngay sau "Sự kiện của tôi": 2 `OutlinedButton` key `backup-export` / `backup-import`; không watch provider lúc build (test cũ chỉ override remote config vẫn chạy); lỗi plugin khi chọn/lưu → SnackBar "Không đọc được file (…)" / "Không xuất được file (…)". (5) `testTall` chuyển từ `user_events_test.dart` sang `test/test_app.dart` để dùng chung.
- Lý do: một serializer cho Export/Import và Drive (D004); tách FileIo để UI/test không phụ thuộc plugin và không đụng dart:io/dart:html (plan §4.7).
- Hệ quả: bước 12 dùng lại `SyncEnvelope.encode()/parse()` cho file Drive và `deviceIdProvider`; đổi schema → tăng `currentSchema` + `parse` đọc nhiều phiên bản.

## 2026-09-10 — D029 — Kiến trúc bước 11: `GoogleAuth` bọc google_sign_in 7, luồng web ≠ Android, cờ `googleSignedIn`
- Bởi: agent
- Quyết định: (1) `lib/data/sync/google_auth.dart`: interface `GoogleAuth { isConfigured, supportsAuthenticate, userChanges, init(restore), signIn(), signOut(), accessToken({interactive}) }` + bản thật `GoogleSignInAuth` trên google_sign_in 7.2.0 (google_sign_in_web 1.1.3, google_sign_in_android 7.2.17); `googleAuthProvider` override trong `main()`, test dùng `FakeGoogleAuth` (`test/test_app.dart`, mặc định cho mọi `testApp`/`testOverrides`). Scope duy nhất `driveAppDataScope` (D007). (2) `initialize()`: web `clientId = Env.webClientId` (thay meta tag trong index.html để client ID vẫn đi qua dart_defines — plan §4.4 đọc theo API mới), `serverClientId` null (web assert); Android `serverClientId = Env.webClientId` (không dùng google-services.json), `clientId` bị bỏ qua. Thiếu `KADE_WEB_CLIENT_ID` → `isConfigured = false`, mục Đồng bộ chỉ hiện cảnh báo (AGENTS §7). (3) Luồng Android: nút app "Đồng bộ với Google" → `authenticate(scopeHint: [drive.appdata])` → `authorizationForScopes` (im lặng) → thiếu thì `authorizeScopes` ngay trong cùng thao tác. Luồng web: `supportsAuthenticate()` false → nút GIS `renderButton` qua `lib/platform/sign_in_button.dart` (conditional import `dart.library.js_interop`, stub ngoài web; cần `google_sign_in_web` là dependency trực tiếp) → sự kiện `authenticationEvents` → hiện email + nút "Cấp quyền Drive" (popup phải xuất phát từ thao tác user — `authorizationRequiresUserInteraction`). (4) `lib/data/sync/auth_provider.dart`: `AuthState{configured, user, driveGranted, busy, error}`, `AuthNotifier` (`signIn`, `driveToken({interactive})`, `signOut`); cờ `googleSignedIn` trong box settings bật khi có user, tắt khi đăng xuất; lúc start `init(restore: cờ)` → `attemptLightweightAuthentication` (Android khôi phục im lặng, web One Tap/FedCM — kết quả về qua stream). `driveGranted` chỉ là trạng thái phiên: web không giữ token qua reload (GIS cache RAM, ≤ 1h) → mỗi phiên web cần 1 click "Cấp quyền Drive"/"Đồng bộ ngay" trước sync; Android token tự làm mới. (5) Lỗi: `GoogleSignInException.canceled` → im lặng; mã khác → hiện "(code: description)" để chẩn đoán SHA-1/package/origin (E002).
- Lý do: google_sign_in 7 tách authentication/authorization và web bắt buộc nút GIS (E003); tách interface để test không cần plugin; giữ quy tắc không hardcode ID (AGENTS §7).
- Hệ quả: bước 12 lấy token qua `ref.read(authProvider.notifier).driveToken()` (im lặng, null → bỏ qua sync); bước 13 xử lý 401/hết hạn bằng `driveToken(interactive: true)` gắn với nút "Đồng bộ ngay" trên web và `clearAuthorizationToken` khi 401. Android minSdk 24 (= mặc định Flutter 3.44, không đổi). `docs/setup-google.md` B.3/B.4 vẫn đúng; B.5 "Kiểm tra nhanh" mô tả nút cũ → cập nhật khi verify.

## 2026-09-10 — D030 — Kiến trúc bước 12: `sync()` qua googleapis Drive v3 sau `DriveStore`, merge/purge pure, "Đồng bộ ngay" thay "Cấp quyền Drive"
- Bởi: agent
- Quyết định: (1) `lib/data/sync/drive_store.dart`: interface `DriveStore { find(token) → RemoteFile?{id, content}; create(token, content) → id; update(token, id, content) }`; bản thật `DriveApiStore` dùng package `googleapis` ^17.0.0 (stack D003) qua `http.BaseClient` gắn `Authorization: Bearer <token>`: `files.list(spaces: appDataFolder, q: name = 'kade_events.json')` (nhiều file cùng tên → lấy `modifiedTime` mới nhất), tải `files.get(alt=media)`, `create`/`update` multipart (`parents: [appDataFolder]`, mimeType application/json). Lỗi API → `DriveException(status, message)` (401 để bước 13 xin lại token). `driveStoreProvider` mặc định bản thật (http chạy cả web lẫn Android), test override `FakeDriveStore` (test_app.dart, mặc định cho `testOverrides`). (2) `lib/data/sync/merge.dart` pure: `mergeEvents(local, remote)` đúng plan §3.10 (chỉ 1 bên → lấy; cả 2 → `updatedAt` lớn hơn; bằng → local; tombstone là 1 bản), `purgeTombstones(events, now, ttl = 90 ngày)`, `sameEvents`, kết quả sắp theo id. (3) `lib/data/sync/sync_provider.dart`: `SyncNotifier.sync({interactive, now})` = `AuthNotifier.driveToken` (null → `noAuth`) → `find` → parse (file hỏng / schema lạ → `failed`, KHÔNG ghi đè) → merge + purge → khác local ⇒ `UserEventsNotifier.replaceAll(merged)` (ghi nguyên trạng `updatedAt`, xóa cứng key đã purge qua `UserEventRepository.replaceAll` — ngoại lệ duy nhất của "không xóa cứng" D025, theo plan §3.5) → khác remote hoặc chưa có file ⇒ `create`/`update` `SyncEnvelope(deviceId, events: merged)` → lưu `lastSyncAt` (box settings, ISO) → `SyncResult{synced|noAuth|busy|failed, changedLocal, uploaded}`. Không upload khi không đổi; đang chạy → `busy`. (4) UI mục Đồng bộ Google: dòng "Đồng bộ lần cuối" + nút "Đồng bộ ngay" (`sync(interactive: true)` → trên web popup xin quyền Drive nằm trong chính click này) thay nút "Cấp quyền Drive" của bước 11; SnackBar "Đã đồng bộ…" / "nhận N thay đổi từ Drive" / lỗi; lỗi sync hiện dưới mục. (5) Trigger tự động (start nếu đã đăng nhập, debounce 5s sau sửa, resume > 15 phút, sau đăng nhập) và xử lý 401 (`clearAuthorizationToken` + xin lại) → bước 13. Purge chấp nhận rủi ro máy > 90 ngày không sync có thể "hồi sinh" bản sống (plan §3.5).
- Lý do: googleapis lo multipart/alt=media và là stack đã chốt; tách `DriveStore` để test sync không cần mạng; merge pure để test 4 case; gộp cấp quyền web vào "Đồng bộ ngay" cho đỡ một click.
- Hệ quả: bước 13 gọi `ref.read(syncProvider.notifier).sync()` (im lặng) từ trigger và `sync(interactive: true)` từ nút; §4.8 mục 4–5 verify bằng 2 trình duyệt / máy cùng tài khoản. Đổi format file Drive = đổi `SyncEnvelope` (D004/D028).

## 2026-09-10 — D031 — Chạy hết plan không dừng chờ verify tay; test tay gom cuối theo phase × Web/Android
- Bởi: user ("session khác sẽ tiếp tục để hết toàn bộ plan… sau cùng t sẽ ngồi test thủ công toàn bộ, chia theo phase và mobile/web")
- Quyết định: Từ bước 13, agent làm tuần tự 13 → 18; mỗi bước verify tự động (analyze, test, build web + apk) rồi commit, KHÔNG dừng chờ user chạy thật. Mục test tay của từng bước ghi vào `docs/manual-test.md` (bảng theo Phase, cột Web / Android, kèm lỗi hay gặp) thay cho PROGRESS "Cần user làm". Trạng thái mới trong bảng PROGRESS: `🧪` = agent xong + test tự động pass, chờ test tay cuối; chỉ ✅ sau khi user test tay báo ok. Chỉ ⏸ khi thiếu thứ user phải cung cấp để code tiếp (hosting/origin bước 15, keystore/Play bước 18) và vẫn làm tiếp bước độc lập. Lựa chọn kỹ thuật thuần (hash vs path URL, GitHub vs Cloudflare Pages…) → agent tự chọn cách đơn giản, ghi DECISIONS, không hỏi. Prompt chuyển giao: `docs/prompts/handover.md`.
- Lý do: user chưa rảnh test từng bước; gom test một lượt cuối hiệu quả hơn.
- Hệ quả: bước 12 ⏸ → 🧪 (mục verify §4.8 4–5 chuyển sang manual-test.md Phase 2, các mục Android của Phase 1–2 cũng ⬜ ở đó). AGENTS.md §2/§3/§4 sửa theo. PROGRESS "Cần user làm" chỉ còn việc console/keystore.

## 2026-09-10 — D032 — Kiến trúc bước 13: `SyncTrigger` nghe auth + sự kiện + vòng đời; 401 bỏ token rồi thử lại đúng 1 lần; "Xóa dữ liệu trên Drive"
- Bởi: agent
- Quyết định: (1) `lib/data/sync/sync_trigger.dart`: `SyncTrigger` trong `syncTriggerProvider` (Provider thường, giữ sống), tạo lúc app start bởi `AppLifecycle` (ConsumerStatefulWidget + `WidgetsBindingObserver` bọc MaterialApp trong `main.dart`). `ref.listen(authProvider)`: xuất hiện user (khôi phục phiên / đăng nhập) hoặc `driveGranted` false→true, không `busy` → `sync()` im lặng. `ref.listen(userEventsProvider)`: đổi (tạo/sửa/xóa/nhập file) → `Timer` 5s (`syncDebounce`, mỗi lần đổi đếm lại) → `sync()`; bỏ qua khi chưa đăng nhập (không hẹn giờ) hoặc `syncProvider.running` (để `replaceAll` của chính sync không gây sync lặp). `onPause/onResume`: mốc rời foreground chỉ ghi ở lần chuyển resumed→khác (Flutter phát hidden/inactive ngay trước resumed, E017); resume sau ≥ 15 phút (`syncResumeAfter`) → sync; resume qua ngày → `ref.invalidate(todayProvider)` (plan §5.6). `clockProvider` (mặc định `DateTime.now`) để test giả giờ. (2) `SyncNotifier.sync`: đặt `running` TRƯỚC khi xin token → trigger nghe auth đổi trong lúc xin quyền thấy busy, không chạy chồng; `DriveException 401` → `AuthNotifier.clearToken` (`authorizationClient.clearAuthorizationToken`, `driveGranted` về false) → `driveToken(interactive: như lần gọi)` → có token mới thì chạy lại đúng 1 lần; không có → `lastError` "Phiên Google hết hạn — bấm Đồng bộ ngay"; 401 lần nữa → báo "Drive 401". `noAuth` giữ nguyên `lastError` cũ. (3) `deleteRemote()` (plan §3.10 "Xóa dữ liệu trên Drive"): find → `files.delete` → xóa `lastSyncAt` → `signOut`; nút TextButton + dialog xác nhận trong mục Đồng bộ; local giữ nguyên, trả chuỗi lỗi hoặc null. (4) Web: có user nhưng không có token im lặng → trigger chỉ được `noAuth`, KHÔNG popup; mỗi phiên web user bấm "Đồng bộ ngay" một lần, sau đó sửa sự kiện tự sync trong ≤ 1h.
- Lý do: nghe provider thay vì gọi trigger rải rác trong UI → mọi đường sửa (kể cả import bước 9) đều được phủ; `running` sớm là cách rẻ nhất tránh race nút ↔ trigger; thử lại 1 lần để không lặp popup.
- Hệ quả: `GoogleAuth.clearToken`, `DriveStore.delete` vào interface (Fake trong test_app.dart thêm `clearedTokens`, `tokenAfterClear`, `badTokens`, nhớ token đã cấp tương tác như cache thật; `sendLifecycle()` giả pause/resume). `authProvider` giờ tạo lúc app start (init + `attemptLightweightAuthentication` chạy ngay khi mở app nếu có cờ, không đợi vào Cài đặt). Bước 16/17 nối "sau sync / sau sửa / resume" vào cùng chỗ (listener trong `SyncTrigger` hoặc provider riêng nghe `userEventsProvider` + `syncProvider.lastSyncAt`).

## 2026-09-10 — D033 — Kiến trúc bước 14: giữ hash URL + GitHub Pages; `?lunar=1` điều hướng theo mùng 1; DayDetail dialog chỉ khi mở từ trong app; rail ≥ 600; hero card + `TodayCardData`
- Bởi: agent (lựa chọn kỹ thuật thuần theo D031)
- Quyết định: (1) Hosting bước 15 = GitHub Pages (miễn phí, cùng repo) → KHÔNG `usePathUrlStrategy`, giữ hash URL `/#/2027/02` (GitHub Pages không rewrite; F5 ở URL sâu vẫn chạy, không cần 404.html). (2) `/YYYY/MM?lunar=1` = đang duyệt theo tháng âm: `lunar_utils.shiftLunarMonth(y, m, ±1)` — sau = tháng âm kế của tháng âm có mùng 1 muộn nhất trong tháng dương, trước = tháng âm liền trước của mùng 1 sớm nhất; tháng dương không có mùng 1 (tháng 2 đôi khi) → tháng âm chứa ngày 1 (sau = tháng kế, trước = chính nó) → luôn đổi tháng dương, không cần state ngoài URL. Picker tháng âm bật cờ; picker dương / "Hôm nay" tắt; nút "Tháng âm" tô nền khi bật. (3) `/d/:date` dùng `pageBuilder`: `DialogPage` (bọc `DialogRoute`, `Dialog` ≤ 720×820) khi bề rộng ≥ 1024 VÀ `state.extra == dayDetailFromApp` (đặt bởi `openDay()` / `pushReplacement` ◀ ▶ khi `canPop`); mở thẳng URL (F5) không có extra → trang riêng, vì go_router dựng stack từ URL nên dưới dialog sẽ trống. (4) `AppShell`: < 600 `NavigationBar`, ≥ 600 `NavigationRail` (plan §4.2 không nói nav cho ≥ 600; rail là cách chuẩn Material). `MonthViewScreen` chọn body theo `layoutOf(MediaQuery width)` (`core/breakpoints.dart`: 600 / 1024): wide = Row 65/35 (`MonthCalendar` + `TodayCard` + tiêu đề "Sắp tới" + `UpcomingList(compact)`), medium = Column 3:2 (lịch trên, `UpcomingList(compact)` dưới), compact = lịch. `UpcomingList` tách khỏi `UpcomingScreen`; compact = không chip, không phụ đề. (5) `lib/data/today_card.dart`: `TodayCardData`/`CardEvent` + `toJson` (date, weekday, solar/lunar, canChi, isOffDay, ≤ 3 events với màu ARGB) — bước 17 dùng lại cho 35 ngày widget; `todayCardProvider` theo `todayProvider` + `dayCellProvider`. (6) Phím: `CallbackShortcuts` + `Focus(autofocus)` chỉ trong `MonthViewScreen` (← → `_shift`, T hôm nay) và `DayDetailScreen` (Esc `_back`) — không đặt ở shell để không nuốt phím của TextField tab Đổi ngày. (7) Cảnh báo web một lần: `platformIsWebProvider` (kIsWeb, test override) + `WebNoticeNotifier` (cờ `webNoticeDismissed` box settings) → `MaterialBanner` đầu MonthView, nút "Bật đồng bộ" (→ /settings) / "Đã hiểu". (8) PWA: manifest name "Kade — Lịch âm dương", short_name Kade, theme `#C62828`, background trắng, `orientation: any`, lang vi; index.html lang vi + description + theme-color; icon giữ mặc định Flutter (repo chưa có logo). `MonthViewScreen` dùng `todayProvider` thay `DateTime.now()` để hôm nay đổi khi resume (D032). `flutter build web --wasm` chạy được (không bắt buộc).
- Lý do: hash URL bỏ hẳn phụ thuộc hosting; điều hướng tháng âm suy từ tháng dương giữ URL là nguồn sự thật (D023); `extra` là cách rẻ nhất phân biệt "có trang bên dưới" mà không đổi scheme URL plan §4.1; rail cho tablet/desktop có chỗ vào Đổi ngày/Cài đặt.
- Hệ quả: `monthPath(y, m, {lunar})`, `openDay(context, date)` thay `context.push(dayPath)` ở MonthView/Sắp tới/hero. Test responsive dùng `setViewport()` (test_app.dart); mặc định 800×600 giờ là layout medium (rail + Sắp tới gọn dưới lịch) → assert text trong lịch phải scope `MonthCalendar` (E018). Bước 15: `--base-href /<repo>/`, origin `https://<user>.github.io`. Bước 17: `TodayCardData.toJson` là schema `days_json`.
