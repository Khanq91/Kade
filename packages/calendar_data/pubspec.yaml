name: calendar_data
description: Dữ liệu lễ VN (nghỉ / kỷ niệm) + quốc tế, YearOverride (nghỉ bù), resolveMonth. Pure Dart.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  lunar_core: any

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.0
