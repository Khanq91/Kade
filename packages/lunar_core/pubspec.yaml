name: lunar_core
description: Engine lịch âm Việt Nam — port 1:1 từ amlich-aa98.js (Hồ Ngọc Đức). Pure Dart, zero runtime deps.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.0
